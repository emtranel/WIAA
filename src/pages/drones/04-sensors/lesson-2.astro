---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import Sidebar from '../../../components/Sidebar.astro';
import LessonNav from '../../../components/LessonNav.astro';
const content = await import('../../../content/drones/04-sensors/lesson-2.md');
const { Content } = content;
const { title, chapter, lesson } = content.frontmatter;
---

<BaseLayout title={title}>
  <div class="container" style="max-width: 1400px; margin: 30px auto; padding: 0 20px;">
    <div style="display: grid; grid-template-columns: 250px 1fr; gap: 24px; align-items: start;">
      <Sidebar currentChapter={chapter} currentLesson={lesson} />
      <div class="content-area">
        <div class="content-header">
          <div class="page-header-bar">
            <div class="tab-buttons">
              <a href="/WIAA/drones" class="tab-btn active">Learning Module</a>
              <a href="/WIAA/drones/instructor" class="tab-btn">Instructor Resources</a>
            </div>
          </div>
          <p class="chapter-label">Chapter {chapter}: Sensors & data collection</p>
          <h1>{title}</h1>
        </div>
        <article class="lesson-content">
          <p class="intro-text">Sensors provide us with data, but what can we do with the data?</p>
          
          <p>Multispectral cameras can be used to determine the health of a plant. Often multispectral cameras contain sensors that capture light reflected in the blue, green, red, red edge, and near-infrared (NIR) areas of the electromagnetic spectrum.</p>
          
          <h2>Key Principle</h2>
          
          <!-- Healthy vs Stressed Comparison -->
          <div class="comparison-grid">
            <div class="comparison-card healthy">
              <div class="comparison-header">
                <h3>Healthy Plants</h3>
              </div>
              <div class="comparison-content">
                <div class="reflectance-item">
                  <span class="label">NIR Light:</span>
                  <span class="value">MORE ↑</span>
                </div>
                <div class="reflectance-item">
                  <span class="label">Red Light:</span>
                  <span class="value">LESS ↓</span>
                </div>
              </div>
            </div>
            
            <div class="comparison-card stressed">
              <div class="comparison-header">
                <h3>Stressed Plants</h3>
              </div>
              <div class="comparison-content">
                <div class="reflectance-item">
                  <span class="label">NIR Light:</span>
                  <span class="value">LESS ↓</span>
                </div>
                <div class="reflectance-item">
                  <span class="label">Red Light:</span>
                  <span class="value">MORE ↑</span>
                </div>
              </div>
            </div>
          </div>

          <p class="explanation-text">The reflectance of light in these two regions of the electromagnetic spectrum can be used to determine the relative health of different plants in a field.</p>
          
          <img src="/WIAA/images/reflectances.jpg" alt="Reflectance Patterns" />
        </article>
        <LessonNav 
          prev={{ href: '/drones/04-sensors/lesson-1', label: 'Previous' }}
          next={{ href: '/drones/04-sensors/lesson-3', label: 'Next' }}
          progress={`${chapter}.${lesson} / 7 Chapters`}
        />
      </div>
    </div>
  </div>
</BaseLayout>

<style is:global>
  :root {
    --isu-red: #c41e3a;
    --isu-gold: #f8c600;
    --text-primary: #1a1a1a;
    --text-secondary: #666666;
    --border-color: #e5e7eb;
    --healthy-color: #059669;
    --stressed-color: #dc2626;
  }

  .lesson-content .intro-text {
    font-size: 1.1em;
    line-height: 1.8;
    color: var(--text-secondary);
    background: linear-gradient(135deg, rgba(196, 30, 58, 0.04), rgba(248, 198, 0, 0.04));
    padding: 24px;
    border-left: 4px solid var(--isu-red);
    border-radius: 8px;
    margin: 28px 0 40px 0;
  }

  .lesson-content h2 {
    font-size: 1.8em;
    font-weight: 700;
    color: var(--isu-red);
    margin: 45px 0 32px 0;
    letter-spacing: -0.5px;
  }

  .lesson-content > p {
    font-size: 1.05em;
    line-height: 1.8;
    color: var(--text-primary);
    margin: 20px 0;
  }

  /* Comparison Grid */
  .comparison-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 28px;
    margin: 40px 0;
  }

  .comparison-card {
    border-radius: 12px;
    overflow: hidden;
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.1);
    transition: all 0.3s ease;
  }

  .comparison-card:hover {
    transform: translateY(-6px);
    box-shadow: 0 16px 40px rgba(0, 0, 0, 0.15);
  }

  .comparison-header {
    padding: 24px;
    color: white;
  }

  .comparison-header h3 {
    font-size: 1.4em;
    font-weight: 700;
    margin: 0;
    letter-spacing: -0.3px;
  }

  .comparison-card.healthy .comparison-header {
    background: linear-gradient(135deg, #059669, #047857);
  }

  .comparison-card.stressed .comparison-header {
    background: linear-gradient(135deg, #dc2626, #991b1b);
  }

  .comparison-content {
    padding: 28px 24px;
    background: white;
  }

  .reflectance-item {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 16px 0;
    font-size: 1.1em;
    border-bottom: 1px solid var(--border-color);
  }

  .reflectance-item:last-child {
    border-bottom: none;
  }

  .reflectance-item .label {
    font-weight: 600;
    color: var(--text-primary);
  }

  .reflectance-item .value {
    font-weight: 700;
    font-size: 1.15em;
    padding: 6px 12px;
    border-radius: 6px;
  }

  .comparison-card.healthy .reflectance-item .value {
    background: rgba(5, 150, 105, 0.1);
    color: var(--healthy-color);
  }

  .comparison-card.stressed .reflectance-item .value {
    background: rgba(220, 38, 38, 0.1);
    color: var(--stressed-color);
  }

  .explanation-text {
    font-size: 1.1em;
    line-height: 1.8;
    color: var(--text-secondary);
    background: linear-gradient(135deg, rgba(5, 150, 105, 0.04), rgba(220, 38, 38, 0.04));
    padding: 24px;
    border-left: 4px solid var(--isu-red);
    border-radius: 8px;
    margin: 40px 0;
  }

  /* Image */
  .lesson-content img {
    max-width: 900px !important;
    height: auto !important;
    display: block !important;
    margin: 50px auto 40px auto !important;
    border-radius: 12px;
    box-shadow: 0 16px 48px rgba(0, 0, 0, 0.12);
    border: 1px solid var(--border-color);
  }

  /* Tab Buttons */
  .tab-buttons {
    display: flex;
    gap: 10px;
    margin-bottom: 20px;
  }

  .tab-btn {
    padding: 12px 20px;
    background: #f5f5f5;
    border: none;
    border-radius: 6px;
    cursor: pointer;
    font-weight: 600;
    transition: all 0.2s;
    text-decoration: none;
    color: var(--text-primary);
    font-size: 0.95em;
  }

  .tab-btn:hover {
    background: #e8e8e8;
  }

  .tab-btn.active {
    background: var(--isu-red);
    color: white;
  }

  .page-header-bar {
    display: none;
  }

  @media (max-width: 1024px) {
    .comparison-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 768px) {
    .lesson-content h2 {
      font-size: 1.5em;
      margin: 35px 0 28px 0;
    }

    .comparison-grid {
      gap: 20px;
      margin: 30px 0;
    }

    .comparison-card {
      border-radius: 10px;
    }

    .comparison-header {
      padding: 18px;
    }

    .comparison-header h3 {
      font-size: 1.2em;
    }

    .comparison-content {
      padding: 20px 18px;
    }

    .reflectance-item {
      padding: 12px 0;
      font-size: 1em;
    }

    .lesson-content img {
      max-width: 100% !important;
      margin: 40px auto 30px auto !important;
    }

    .page-header-bar {
      display: block;
      margin-bottom: 20px;
      padding-bottom: 15px;
      border-bottom: 2px solid var(--isu-gold);
    }

    .tab-buttons {
      flex-direction: column;
    }

    div[style*="grid-template-columns"] {
      display: flex !important;
      flex-direction: column-reverse !important;
      gap: 16px !important;
    }
  }
</style>